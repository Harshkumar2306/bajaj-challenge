/** @type {import('next').NextConfig} */
const nextConfig = {
  // No rewrites or custom headers needed since the backend is hosted externally and handles its own CORS.
};

export default nextConfig;
